{
  "name": "biewer-website",
  "version": "1.0.0",
  "description": "Website für die Pension Biewer in Ockfen",
  "main": "server/app.js",
  "scripts": {
    "lint": "echo '=> linting' && eslint -c ./.eslintrc.json src/js/**.js && eslint -c ./.eslintrc.json server/**.js",
    "css": "echo '=> converting less to css' && mkdir -p public/style && lessc src/style/style.less public/css/style.css",
    "js": "echo '=> making js files ready for browser' && mkdir -p public/js && cp node_modules/bootstrap/dist/js/bootstrap.min.js public/js/bootstrap.min.js && cp src/js/**.js public/js/",
    "img": "echo '=> copying the assets' && mkdir -p public/assets && cp src/assets/* public/assets/",
    "minify": "echo '=> minify all ressources' && minify public/js/main.js > public/js/main.min.js && rm public/js/main.js && mv public/js/main.min.js public/js/main.js && mkdir -p public/style && lessc --clean-css src/style/style.less public/style/style.css",
    "clean": "echo '=> cleaning project' && rm -rf node_modules && rm -rf public",
    "build": "echo '=> building project' && npm run lint && npm run css && npm run js && npm run img && npm run minify",
    "debug": "echo '=> building debug project' && npm run lint && npm run css && npm run js && npm run img",
    "start": "echo '=> starting server' && node server/app.js"
  },
  "author": "Marc Hein",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.2",
    "express-form-post": "^1.4.2",
    "jquery": "^3.3.1",
    "moment": "^2.22.1",
    "popper.js": "^1.14.3",
    "pug": "^2.0.0-rc.4"
  },
  "devDependencies": {
    "babel-minify": "latest",
    "bootstrap": "latest",
    "browserify": "latest",
    "eslint": "latest",
    "invariant": "^2.2.2",
    "less": "latest",
    "less-plugin-clean-css": "latest"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/marchein/biewer.git"
  },
  "bugs": {
    "url": "https://github.com/marchein/biewer/issues"
  },
  "homepage": "https://github.com/marchein/biewer#readme"
}
